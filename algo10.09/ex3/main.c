#include <stdio.h>
#include <stdlib.h>

int main()
{
   float x, y, potencia;
   printf("informe o valor de X:");
   scanf("%f", &x);
   printf("informe o valor de Y:");
   scanf("%f", &y);
   for(int i = 1; i <= y; i++){


   }
   printf("o resultado eh: %f", potencia);
    return 0;
}
